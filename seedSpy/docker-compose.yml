version: '3.8'

services:
  app:
    container_name: seed_spy
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8080:8000"
    networks:
      - seed_spy_network

networks:
  seed_spy_network:
    driver: bridge